# 项目概览（中文）

这个仓库展示 GALEX 在 **The Pokémon Company - PTCG AI Battle Challenge Simulation**
的参赛方案：**264 / 6,807，前 3.9%，银牌**。
[查看证书](evidence/galex_certificate.png)。

## 项目解决什么问题

让 AI 从已有对局里学习：面对当前局面，应该选择哪些合法选项、选几个。
比赛环境提供候选选项，模型负责打分和选择。同一回合可以有多次请求，
模型每次都根据更新后的局面重新判断，而不是事先固定一整套动作。

最终方案采用**行为克隆（BC，属于模仿学习）**，没有使用 PPO 自对弈。
基础策略结合注意力网络的门控修正；另一个预测分支处理选择数量。
训练时的三个辅助分支学习回合内动作依赖，不会把未来信息交给推理模型。

## 现在可以直接看到和运行什么

- [模型计算](ptcg_agent/model.py)：注意力、门控残差和数量预测。
- [特征处理](ptcg_agent/features.py)：卡牌、场面、公开历史、资源和战术。
- [决策入口](ptcg_agent/agent.py)：接收请求并返回选项索引。
- [训练参考代码](training_reference/README.md)：辅助标签和训练流程；部分依赖仍缺失。
- 最终提交内的原样权重，约 5.6 MB，不需要另行下载模型。

V67、V76 是内部迭代编号；V76 沿用成熟架构，从随机初始化训练全部参数。
不需要理解这些编号才能阅读或运行这个项目。

## 在 Windows 上运行小示例

准备 Python 3.12。下载并解压仓库后，打开包含 `README.md` 的文件夹，
在文件夹地址栏输入 `powershell` 并按回车。先创建隔离环境：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m examples.decision_demo
```

示例会加载真实权重，给三个**人工构造的选项**打分，再打印选择结果。
它不需要 GPU、API 密钥或调用 GPT/Grok。
如果提示找不到 `py` 或 Python 3.12，需要先安装对应 Python，不能直接跳过这一步。

这不是完整游戏演示。在缺少比赛 `cg` SDK 的环境里，卡牌元数据采用原实现的
默认回退值；因此示例跑通不等于复现比赛胜率。

## 验证做到了哪一步

24 项验证工具测试和 13 项实际模型测试在本地通过。另用 37 个合成请求
逐个比较拆分版与原提交：选项与历史状态一致，检查到的分数向量差异为零。
原始权重的全部 133 组张量也完全一致。

完整训练仍缺少上游特征模块、训练数据和确切检查点，暂时不声称能够一键重训，
也没有重新跑正式模拟器或排行榜。详见[运行与复现边界](docs/REPRODUCIBILITY.md)
和[验证记录](docs/VALIDATION.md)。

## 开发方式

本仓库由 **Yuzhen Guan（Alex）** 维护，展示的是 GALEX 的团队比赛成绩。
开发基于公开基线思路与参考实现，结合 GPT、Grok 等大语言模型讨论状态／动作表示、
战术特征和组件整合，并参与数据准备、实验比较及提交调试。
大语言模型用于辅助开发，不是对战时的决策后端。

赛后模块化整理、文档与测试借助 Codex 完成，不能把这些赛后工作写成比赛提分原因。
源码与权重已按参赛者转达的公开许可发布，未擅自添加覆盖所有内容的开源许可证。
详见[来源说明](docs/PROVENANCE.md)和[发布说明](docs/RELEASE.md)。
