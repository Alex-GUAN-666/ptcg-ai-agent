# 项目概览（中文）

这个仓库展示 GALEX 在 **The Pokémon Company - PTCG AI Battle Challenge Simulation**
的参赛方案：264 / 6,807，前 3.9%，银牌。

## 项目解决什么问题

从已有对局记录中学习：在当前局面下，应该选择哪些合法动作、选几个。
比赛环境提供候选动作和数量限制，模型负责打分与选择。
同一个回合可能包含多次决策，模型在每次决策请求时重新推理。

最终方案使用**行为克隆（BC）**：用示范动作作为监督信号训练策略。
它属于模仿学习；最终模型没有使用 PPO 自对弈训练。

## 方法与迭代

| 环节 | 做法 |
| --- | --- |
| 数据选择 | 导师介绍了约 15 天的滚动对局窗口；V76 数据脚本针对编号以 `312` 开头的目标卡组提取决策 |
| 状态表示 | 综合卡牌、场面、历史、资源，以及伤害、能量费用和进化等战术特征 |
| 动作选择 | 基础策略分数叠加注意力网络的门控修正，另用数量预测头决定选几个动作 |
| 训练辅助 | 使用同回合后续动作生成辅助标签，只用于训练，不作为推理时可见的未来信息 |
| 推理导出 | 移除训练辅助头，用 NumPy 加载嵌入权重，在每次请求中返回候选动作的索引 |

导师总结的演进路线是：V20 引入注意力机制，V40 增加战术特征，
V67 增加三个训练辅助头，V76 沿用 V67 架构并将所有参数从头训练。

会议里一个有价值的经验是：**更新的对局数据不一定带来更强的策略**。
导师观察到，中期训练的一些模型比前期、后期更强，并将其解释为示范质量和
对手策略变化的影响。这是导师的实验观察，目前没有在本仓库重新进行对照实验。

详见[方法与版本记录](docs/METHOD.md)。会议中的动作预测准确率是对示范动作的
一致程度，不是对战胜率，也不是排行榜分数。

## 成果与验证

- 团队成绩：264 / 6,807，前 3.9%，银牌；[查看证书](evidence/galex_certificate.png)。
- 保留的提交文件包含 1,402,487 个 float32 参数值；这是导出文件的实际统计口径。
- 24 项验证工具单元测试通过；另有 12 项针对原始提交的本地合成输入检查通过。
- 这些检查覆盖文件完整性、选择数量和索引等接口行为，不等于复现正式对局表现。

## 当前可运行范围

本仓库公开方法说明、成绩证明和验证工具。原始 agent 代码与权重的公开许可尚待确认，
所以下载仓库后不能直接运行训练好的玩家；完整训练还缺少部分上游依赖。

使用 Python 3.12，在仓库根目录运行：

```bash
python -m unittest discover -s tests -v
```

如已持有获准使用的原始提交，可按[运行说明](docs/REPRODUCIBILITY.md)执行文件校验
和可选的本地推理检查。更多记录见[验证说明](docs/VALIDATION.md)。

## 来源与维护

本仓库由 Yuzhen Guan（Alex）维护，用于展示 GALEX 的参赛方案。
核心 agent 与训练方案由导师 **kaggle竞赛圈** 提供；赛后文档和验证工具借助 AI 整理。
团队成绩、导师方案与赛后仓库工作分别归属，详见[来源说明](docs/PROVENANCE.md)。
